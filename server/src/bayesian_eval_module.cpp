#include "server/eval/bayesian_eval_module.h"

// AGENT-CTX: Task 9 skeleton — all on_* methods are no-ops; full math ships in Task 10.
// The file exists so CMake can link bayesian_module_tests and the server binary.

namespace anjeer::server::eval {

// All implementations intentionally empty until Task 10.

} // namespace anjeer::server::eval
