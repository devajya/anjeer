# Anjeer metrics — 2026-08-14

Target: `local (http://localhost:10000)`, i7-12700H / 20 threads / 16 GB.
Consolidated from several runs on the same day, since `make metrics` overwrites
this file and the capacity and CPU figures come from different ramp lengths.
Re-run with `make metrics` to regenerate a single-run version.

| Metric | Value | Detail |
|---|---|---|
| OrderBook throughput | 13.4M orders/sec | `game_scale`: 74.6 ns/order. Also match_heavy 18.8M, depth_churn_4 10.5M, depth_churn_16 10.1M, depth_churn_64 8.3M, depth_churn_256 5.5M |
| Order to ack | p50 20.52 ms / p99 21.50 ms | n=240 of 240 (100% acked), p95 21.25 ms, mean 16.93 ms, max 21.55 ms, single lobby |
| Max concurrent lobbies | ≥24 | 96 concurrent players, ack p99 21.55 ms, ramp ceiling reached without degrading |
| Server CPU per lobby | 1.75% of one core | idle 0.0%, 28.0% at 16 lobbies (64 players), linear from 3 lobbies up |
| Server RSS per lobby | 0.5 MB | baseline 15 MB, 23 MB at 16 lobbies |
| Render per market burst | 3.409 ms/update | 1008 commits, max 9.821 ms, actual/base 0.846 |
| msgpack vs JSON bytes | 122 B vs 122 B (+0%) | 240 frames each to idle observers. The msgpack socket received 0 binary frames, so the subprotocol was negotiated but never applied. See docs/performance.md |
| Lighthouse performance | not collected | `@lhci/cli` is not installed; `make lighthouse` would run an unrelated npm package named `lhci` |

Users (total / daily active) are recorded separately.

Analysis, extrapolation to the production t3.micro, and the msgpack defect are in
`docs/performance.md`.
